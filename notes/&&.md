```jsx
{sender === 'robot' && <img src="robot.png" width="30"/>}
```
This above code works in the following way : \
if sender === 'robot' then do the img thing
it is like value1 && value2\
if value1 is true then the result of the operation is value2